import { createStyles } from '@material-ui/core';

export default () => createStyles({
  wrapper: {
    textAlign: 'center',
    width: '100%',
    height: '100%',
  },
  input: {
    marginTop: '40px',
  },
  text: {
    marginTop: '80px',
  },
});
